Install fractal CLI with:

```
npm i -g @frctl/fractal
```

Build the CSS
```
npm run build-sass
```

Build the JavScript:
```
npm run build-js
```

Start the server
```
fractal start --sync
```

Learn more at [https://fractal.build](https://fractal.build)
