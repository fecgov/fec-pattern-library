{
  "label": "Multi select",
  "title": "Dropdown: multi-select",
  "notes": "A dropdown allows users to select one option from a list. Users can apply multiple options.<br>If you need to align the panel to the right edge, use .dropdown__panel--right."
}
