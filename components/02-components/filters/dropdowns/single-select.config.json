{
  "label": "Single select",
  "title": "Dropdown: single-select",
  "notes": "A dropdown allows users to select one option from a list.<br>If you need to align the panel to the right edge, use .dropdown__panel--right."
}
